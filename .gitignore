# Real config carries a secret token — never commit it.
/config.json
.invoicesup-agent-state.json

# Built binaries
/invoicesup-agent
/invoicesup-agent.exe
*.exe

# Build output (cross-compiled binary + installer artifacts)
/dist/
/installer/Output/

# Code-signing material — never commit
*.pfx
*.p12
